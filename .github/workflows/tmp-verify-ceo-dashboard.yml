name: 一時調査・CEOダッシュボード本番確認

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: 本番URLをcurlで確認（詳細）
        run: |
          echo "=== HTTPステータス・ヘッダー ==="
          curl -sS -D - -o /tmp/body.html https://ceo-dashboard-alpha-livid.vercel.app/ || echo "curl failed: $?"
          echo "=== レスポンスボディ（先頭2000文字） ==="
          head -c 2000 /tmp/body.html
